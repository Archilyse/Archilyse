import React from 'react';
import { LoadingIndicator } from 'archilyse-ui-components';

const LoadingData = () => (
  <div data-testid="main-loading-indicator" className="selected-view no-data">
    <LoadingIndicator />
  </div>
);

export default LoadingData;
