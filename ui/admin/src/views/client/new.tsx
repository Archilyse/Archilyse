import React from 'react';
import { EntityView } from '../../components';
import formFields from '../../common/forms/client';

const Client = () => <EntityView fields={formFields} entity={{}} parent={{}} context="client" />;

export default Client;
