import React from 'react';

const SimulationViewer = ({ buildingId, unitId, simType, displayMode, unitClientId }) => {
  return <div className={`simulationViewer ${simType}`} />;
};

export default SimulationViewer;
